# Node.js-app
Real-Time Project: CI/CD Pipeline for a Node.js App with Docker &amp; Jenkins
We’ll build a Node.js app → push to GitHub → Jenkins will:

Pull code from GitHub

Run tests

Build a Docker image

Push image to Docker Hub

Deploy container on server

Project Structure (GitHub repo)
nodejs-jenkins-docker/
 ├── app.js
 ├── package.json
 ├── Dockerfile
 └── Jenkinsfile
